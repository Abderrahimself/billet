# governance layer — private (O15)
mvp.md
devops.md
CLAUDE.md
POST-MVP.md
.claude/
docs/reviews/

# local env — real values never committed (O5); .env.example stays tracked
.env
.env.local
.env.*.local

# dev signing keys and any PEM material — never committed (O5)
secrets/
*.pem

# Python (services/*)
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
